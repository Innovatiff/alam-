{
  "name": "innovatiff-site",
  "version": "1.0.0",
  "private": true,
  "description": "Innovatiff marketing website: static, zero-dependency build.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "serve": "npx --yes http-server . -p 8080 -c-1 -s",
    "dev": "node build.mjs && npx --yes http-server . -p 8080 -c-1"
  },
  "license": "UNLICENSED"
}
